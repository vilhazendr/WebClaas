/*
 * WebClass backend configuration.
 * After deploying the Node.js server, replace the URL below with:
 * https://YOUR-BACKEND.onrender.com/api
 */
window.WEBCLASS_API_URL = window.WEBCLASS_API_URL || 'https://YOUR-BACKEND.onrender.com/api';
